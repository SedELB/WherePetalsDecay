import { CommunicationService } from './communication.service';

export class EditorPageService {
    constructor(private readonly communicationService: CommunicationService) {}

    //jattend que le ComuunicationService soit fait

}